import Blockquote from '@components/Blockquote';
import Button from '@components/Button'
import TranslatedBy from '@components/TranslatedBy'
import Pagination from '@components/Pagination'

## 部屬你的 Next.js 程式

<Pagination
    hrefs={[
      '/learn/basics/deploying-nextjs-app',
      '/learn/basics/deploying-nextjs-app/setup',
      '/learn/basics/deploying-nextjs-app/github',
      '/learn/basics/deploying-nextjs-app/deploy',
      '/learn/basics/deploying-nextjs-app/platform-details',
      '/learn/basics/deploying-nextjs-app/other-hosting-options',
      '/learn/basics/deploying-nextjs-app/finally',
    ]}
/>

### 其他供應商

Next.js可以部署到任何支援Node.js的主機供應商。

如果您迄今為止已經按照說明進行操作，您的`package.json`應該有下列的`build`和`start`指令：

```json
{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start"
  }
}
```

在您的主機供應商上運行一次`build`指令，該指令將在`.next`資料夾中構建生產應用程式。

```bash
npm run build
```

構建完成後，執行`start`指令會啟動一個混合用的Node.js伺服器，提供靜態生成、伺服端渲染的頁面及API路由。

```bash
npm run start
```

<Blockquote>
**小技巧：** 您可以通過在`package.json`中更新`start`指令來自定義接受`PORT`參數，如下所示：`"start": "next start -p $PORT"`。
</Blockquote>

這就是全部了！如果您對於部署Next.js有任何問題，歡迎在<a href="https://github.com/vercel/next.js/discussions" target="_blank">GitHub討論區</a>向我們的社群尋求幫助。

<Button
    content="返回 ←"
    url="/learn/basics/deploying-nextjs-app/platform-details"
    basicStyle="text-white bg-[#0070f3]"
    hoverStyle="hover:opacity-90"
/>

<Button
    content="下一個 →"
    url="/learn/basics/deploying-nextjs-app/finally"
    basicStyle="text-white bg-[#0070f3]"
    hoverStyle="hover:opacity-90"
/>

<TranslatedBy githubUserNames={["Dalufishe"]} />